# Deploy Backend — placeholder
# Full workflow defined in US-5.
# This file must exist to satisfy the monorepo skeleton acceptance criterion (§ 4.1).
name: deploy-backend

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: placeholder
        run: echo "Backend deploy workflow — to be implemented in US-5"
