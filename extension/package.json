{
  "name": "cortex-clip-extension",
  "version": "1.0.0",
  "description": "Chrome MV3 extension for Cortex Second Brain — clip current tab to your library.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.1.0"
  }
}
